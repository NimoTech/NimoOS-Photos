# AMD iGPU (ROCm) device mapping; gfx1151 (AI Max+ 395 / Strix Halo) needs an HSA override
services:
  immich-machine-learning:
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    environment:
      HSA_OVERRIDE_GFX_VERSION: "11.5.1"
